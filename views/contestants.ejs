<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contestants</title>
    <link rel="stylesheet" href="css/dashboard.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Barlow+Condensed|Barlow:600|Rasa:300,500" rel="stylesheet">
    <style>
        /* Main Bar Styling */
        .main-bar {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            background-color: #f0f4f8;
        }

        /* Contestant Content */
        .contestant-content {
            background-color: #fff;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 800px;
        }

        /* Header Styling */
        .contestant-content h1 {
            font-size: 2rem;
            color: #333;
            text-align: center;
            margin-bottom: 1.5rem;
        }

        /* Table Container */
        .contestant-table {
            width: 100%;
            overflow-x: auto;
        }

        /* Table Styling */
        .contestant-table table {
            width: 100%;
            border-collapse: collapse;
            margin: 0 auto;
            font-size: 1rem;
            background-color: #fff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        /* Table Rows */
        .contestant-table tr {
            border-bottom: 1px solid #e0e0e0;
            transition: background-color 0.3s ease;
        }

        .contestant-table tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .contestant-table tr:hover {
            background-color: #f1f1f1;
        }

        /* Table Data */
        .contestant-table td {
            padding: 1rem;
            text-align: left;
            color: #333;
            vertical-align: middle;
        }

        /* Image Styling */
        .contestant-table td img {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            object-fit: cover;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .contestant-content {
                padding: 1rem;
            }

            .contestant-content h1 {
                font-size: 1.5rem;
            }

            .contestant-table table {
                font-size: 0.9rem;
            }

            .contestant-table td {
                padding: 0.75rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <nav class="nav-bar"></nav>
        
        <aside class="aside-bar">
            <!-- Add your navigation items here -->
        </aside>

        <main class="main-bar">
           <div class="contestant-content">
            <h1>Contestants</h1>
            <div class="contestant-table">
                <table>
                    <tr>
                        <th>Photo</th>
                        <th>First Name</th>
                        <th>Middle Name</th>
                        <th>Last Name</th>
                        <th>Position</th>
                        <th>Party</th>
                    </tr>
                    <% contestants.forEach(function(contestant) { %>
                      <tr>
                        <td>
                          <img src="data:image/jpeg;base64,<%= contestant.photo.toString('base64') %>" alt="Contestant Photo">
                        </td>
                        <td><%= contestant.firstname %></td>
                        <td><%= contestant.middlename %></td>
                        <td><%= contestant.lastname %></td>
                        <td><%= contestant.position_id %></td>
                        <td><%= contestant.party %></td>
                      </tr>
                    <% }) %>
                </table>
            </div>
           </div>
        </main>
    </div>
</body>
</html>
